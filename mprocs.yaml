procs:
  terminal:
    shell: "fish"
  git:
    shell: "gitui"
  dev:
    shell: "just dev"
    stop: "SIGKILL"
  todo:
    cwd: "docs"
    shell: "flatlite-cli"
  build-release:
    shell: "just build-release"
    autostart: false
  deploy-itch:
    shell: "just deploy-itch"
    autostart: false
